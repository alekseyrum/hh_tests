// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
//
// HH.ru : Linux Продвинутый уровень
//
// - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Как понять из вывода команды dmesg , что произошла ошибка с жёстким диском?  

правильный ответ:
++ 		: ata2.00: failed command: READ DMA EXT

ошибки:
-- sda: Write Protect if off
-- system: Starting Daily apt upgrade and clean activities...
-- EXT4-fs (sda
-- :mounted filesystem with ordered data mode

*Этот вывод от драйвера низкоуровневого интерфейса (libata) сообщает о том, что ядро операционной системы отправило команду на чтение (READ DMA EXT), но жесткий диск не смог её выполнить и ответил ошибкой ввода-вывода (I/O error).
 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы хотите получить сообщения только текущего сеанса загрузки с уровнем error и выше. Что использовать?

правильный ответ:
++ 		: journalctl -p err -b

ошибки:
-- journalctl --since=today
-- journalctl -b -f
-- journalctl -k -p 3
-- journalctl -u default.target -p error

-b (boot) — указывает journalctl выводить сообщения только текущего сеанса загрузки системы (с момента последнего включения или перезагрузки).
-p err (priority) — фильтрует логи по уровню важности. Значение err (или 3) выводит сообщения с уровнем error и выше (то есть сам error, а также критические crit, alert и emerg)

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Скрипт читает список имён файлов из текстового файла filelist.txt, в котором строки могут содержать пробелы. Как безопасно обойти этот список в цикле?

правильный ответ:
++ 		: while IFS= read -r line; do echo "$line"; done < filelist.txt

ошибки:
-- for file in $(cat filelist.txt); do echo "$file"; done
-- while read line; do echo "$line"; done < filelist.txt
-- cat filelist.txt | while read line; do echo $line; done
-- xarg -n 1 echo < filelist.txt

IFS= (Internal Field Separator) — очистка переменной разделителя полей. Без этого read автоматически обрезал бы начальные и конечные пробелы или знаки табуляции в именах файлов.
read -r — флаг -r отключает обработку обратных слэшей (\). Если в имени файла есть символ \, он прочитается как обычный символ, а не как экранирование.
"$line" — кавычки вокруг переменной обязательны. Они защищают имя файла от разделения на отдельные слова при передаче в команду echo или любую другую утилиту.
< filelist.txt — перенаправление ввода напрямую в цикл. Это эффективнее и безопаснее, чем использование конвейера (|).

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой способ вызова Bash-скрипта обеспечивает выполнение в текущем shell , а не в отдельном процессе?

правильный ответ:
++ 		: source ./script.sh

ошибки:
-- ./script.sh
-- bash ./script.sh
-- sh ./script.sh

*source (или встроенная команда .) считывает и выполняет команды из указанного файла прямо внутри текущего процесса командной оболочки (current shell).

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Что может произойти, если директива client_max_body_size не задана или имеет слишком большое значение?

правильный ответ:
++ 		: Злоумышленник может инициировать отказ в обслуживании, отправив очень большой запрос

ошибки:
-- Браузер будет перенаправлен на другой хост при загрузке файлов
-- TLS-соединения будут завершаться с ошибкой при передаче тела запроса
-- Файлы будут игнорироваться, если превышают ограничение по inode
-- Nginx не сможет читать переменные запроса в POST-методе

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой параметр конфигурационного файла PostgreSQL позволяет разрешить подключение по сети?

правильный ответ:
++ 		: listen_addresses

ошибки:
-- password_encryption
-- max_connections
-- port

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
На сервере с 8 ядрами команда uptime показывает load average: 14.20 15.00 14.80. Как можно интерпретировать эти значения?

правильный ответ:
++ 		: Средняя нагрузка превышает количество доступных ядер - CPU перегружены

ошибки:
-- Значения load average находятся в пределах нормы, загрузка сбалансирована
-- Такая нагрузка характерна для дисковых операций, а не CPU
-- Значения не учитывают многопоточность, нагрузка не превышает порог
-- Низкий load average свидетельствует об ограничении по памяти

*Показатель Load Average (LA) отражает среднее количество процессов, которые находятся в состоянии готовности к выполнению и ждут своей очереди на CPU (а также процессов в непрерывно ожидаемом состоянии, например, из-за дискового ввода-вывода).
Правило идеальной загрузки: Для системы с 8 ядрами значение LA, равное 8.0, означает, что процессор загружен ровно на 100% — все ядра постоянно заняты работой, но очереди из процессов еще нет. 
В данной ситуации: Значения 14.20 15.00 14.80 (за последние 1, 5 и 15 минут соответственно) почти в два раза превышают количество ядер (8). Это означает, что процессор жестко перегружен. В среднем около 6-7 процессов постоянно стоят в очереди и ждут, пока освободится ядро процессора, из-за чего система может ощутимо «тормозить».

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
На сервере наблюдается замедленная работа приложений. Вам нужно в реальном времени определить, какие процессы загружают процессор и сколько оперативной памяти используется. Какую команду следует применить?

правильный ответ:
++ 		: top

ошибки:
-- ps aux
-- grep cpuinfo
-- du -sh
-- df -h

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Не сработало задание cron, использующее переменную среды, определённую в ~/.bashrc . Почему?

правильный ответ:
++ 		: Cron не загружает пользовательские переменные из ~/.bashrc

ошибки:
-- Script запускается не из домашней директории
-- Нет прав на запись в crontab
-- Переменные среды не поддерживаются в cron

*Демон cron запускает задания в неинтерактивной оболочке (non-interactive shell) без привязки к терминалу.
Поскольку cron не инициализирует интерактивную оболочку, среда окружения внутри него получается крайне минималистичной. В ней нет ваших кастомных переменных, а переменная PATH обычно сброшена до стандартного минимума (вроде /usr/bin:/bin).

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы настроили таймер на ежедневный запуск сервиса с типом oneshot и добавили в сервис RemainAfterExit=true . Какое поведение будет наблюдаться?

правильный ответ:
++ 		: systemctl будет считать, что сервис активен до следующего вызова, даже после его завершения

ошибки:
-- Сервис будет автоматически перезапускаться при истечении таймера
-- Флаг RemainAfterExit игнорируется при Type=oneshot
-- Таймер будет выполнять сервис только при наличии активного пользователя в системе
-- RemainAfterExit=true приведёт к тому, что таймер не завершится корректно

*RemainAfterExit=true принудительно заставляет systemd считать сервис активным (active (exited)) даже после того, как его основной процесс успешно завершился.
Поведение с таймером: На следующий день, когда сработает ежедневный таймер, он попытается снова запустить этот сервис. Однако, поскольку для systemd сервис всё еще числится «активным», повторного выполнения команд из ExecStart= не произойдет. Таймер просто проигнорирует запуск, так как сервис, с точки зрения системы, уже запущен. (Чтобы ежедневный таймер работал корректно для oneshot-сервиса, параметр RemainAfterExit должен быть равен false, либо вообще отсутствовать).

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какое действие предпринимает systemd , если unit имеет OnFailure=emergency.target , а сервис завершается с ошибкой?

правильный ответ:
++ 		: Активирует emergency.target 

ошибки:
-- Перезагружает сервис автоматически.
-- Пропускает ошибку.
-- Откладывает запуск зависимых сервисов

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
При отладке зависимостей между юнитами systemd вы хотите визуализировать, какие службы блокируют или откладывают загрузку других. Что применить?

правильный ответ:
++ 		: systemd-analyze critical-chain

ошибки:
-- systemd-tmpfiles --cat-config
-- ls /etc/systemd/system/
-- systemctl show --all
-- journalctl -k -b

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Конфигурация OpenSSH содержит следующие строки:
PasswordAuthentication no
Match User backup
	PasswordAuthentication yes
Пользователь backup успешно аутентифицируется по паролю. В чем причина?	

правильный ответ:
++ 		: Правило Match User переопределяет глобальные праметры и разрешает парольную аутентификацию только для указанного пользователя

ошибки:
-- Если пользователь задан в Match, глобальные правила игнорируюся полностью
-- sshd не применяет дируективу PasswordAuthentication без AllowUsers
-- Глобальная директива PasswordAuthentication no применяется только для root
-- Match-блоки не поддерживают аутентификацию по паролю без PAM

*Блок Match в конфигурационном файле sshd_config служит для введения условных (специфичных) правил, которые применяются только тогда, когда критерий проверки совпадает (в данном случае, когда подключается пользователь с именем backup).
Настройки, объявленные внутри блока Match, переопределяют (замещают) глобальные директивы, которые были указаны в начале файла.
Таким образом, для всех пользователей в системе вход по паролю полностью запрещен (PasswordAuthentication no), но как только система видит пользователя backup, для него вступает в силу персональное разрешение (PasswordAuthentication yes).

*PAM (Pluggable Authentication Modules) — это системная подсистема в Linux и других Unix-подобных операционных системах, которая отвечает за аутентификацию пользователей.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
На сервере с включенным SELinux приложение webapp запущено под собственным доменом webapp_t. В конфигурации SELinux для webapp_t разрешён доступ только к определенным файлам в каталоге /var/www/webapp, а также ограничены сетевые подключения. При этом в системе назначены Linux capabilities CAP_NET_BIND_SERVICE и CAP_DAC_OVERRIDE для бинарника webapp.
При текущей конфигурации существует риск нарушения изоляции и несанкционированного доступа к критическим файлам вне разрешенного каталога.
Что из нижеперечисленного наиболее вероятно объясняет причину уязвимости?

правильный ответ:
++ 		: Назначение CAP_DAC_OVERRIDE позволяет приложению обходить стандартные проверки прав доступа файловой системы, игнорируя ограничения SELinux на уровне DAC, что приводит к возможному доступу к файлам вне /var/www/webapp

ошибки:
-- SELinux ограничивает только доступ к файлам, а capabilities управляют только сетевыми ресурсами, следовательно риск нарушения изоляции исключён
-- SELinux политика по умолчанию игнорирует права capabilities, поэтому назначение CAP_DAC_OVERRIDE не влияет на безопасность
-- Разрешение CAP_NET_BIND_SERVICE позволяет приложению прослушивать сетевые порты ниже 1024, что создает прямую уязвимость в SELinux политике
-- Использование собственного домена webapp_t полностью изолирует приложение, но назначение любых capabilities приводит к отключению SELinux для данного процесса

*SELinux (Security-Enhanced Linux) — это встроенная в ядро Linux система безопасности, которая реализует механизм мандатного управления доступом (MAC — Mandatory Access Control).
*В ядре Linux проверка прав доступа происходит строго по очереди и состоит из двух этапов:
DAC (Discretionary Access Control) — стандартная дискреционная модель прав (владелец, группа, chmod, chown).
MAC (Mandatory Access Control) — мандатная модель (в данном случае SELinux), которая проверяет контексты и политики безопасности.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы анализирует запись в /etc/fstab:
/dev/sdb1 /backup ext4 defaults 0 0
Что означает эта запись для проверки файловой системы при загрузке?

правильный ответ:
++ 		: Проверка файловой системы будет отключена

ошибки:
-- Файловая система будет проверяться каждый раз
-- Будет выполнена автоматическая проверка после загрузки
-- Файловая система будет проверяться при сбоях

*очередность проверки файловой системы на наличие ошибок утилитой fsck при загрузке операционной системы определяются самым последним (шестым) полем
Пятое поле, которое здесь также равно 0, отвечает за резервное копирование утилитой dump.
Четвертое поле в файле /etc/fstab определяет опции монтирования файловой системы. Это список параметров, разделенных запятыми (без пробелов), которые указывают ядру Linux, какие правила, ограничения и права доступа нужно применить к этому разделу при его подключении.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
После добавления новой строки в /etc/fstab и перезагрузки сервер загрузился в emergency mode . Какова наиболее вероятная причина потери доступа к данным на разделе /mnt/data ?

правильный ответ:
++ 		: Неправильный UUID в записи fstab

ошибки:
-- Отсутствие монтируемой директории
-- Несовместимая файловая система
-- Ошибочный режим монтирования ( ro вместо rw )

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой вывод команды journalctl -xe наиболее вероятно указывает на проблему с монтированием файловой системы?

правильный ответ:
++ 		: system[1]: Dependency failed for /mnt/data

ошибки:

-- kernel: eth0: link is up, 1000 Mbps full-duplex
-- sshd[1234]: Accepted password for user1 from 192.168.1.15 port 54321
-- Process CRON[2248] exited with code 0

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Как вывести логи, записанные между 11:00 и 12:00 сегодняшнего дня?

правильный ответ:
++ 		: journalctl --since "11:00" --until "12:00"

ошибки:
-- journalctl --at "11:00-12:00"
-- journalctl -S "11:00" -U "12:00"
-- journalctl --from "11:00" --to "12:00"

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Проанализируйте результат следующего скрипта:
#!/bin/bash
FILES="/var/log/syslog /var/log/messages"
for f in $FILES
do
	test -f $f && echo "$f found"
done

правильный ответ:
++ 		: Выведет только существующие файлы

ошибки:
-- Всегда выведет обе строки
-- Скрипт всегда завершится с ошибкой
-- Скрипт вызовет ошибку, если файлов нет

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы хотите создать скрипт, который выводит по одному имени на строку всех файлов в текущем каталоге, начинающихся на file и заканчивающихся на .txt. Какой фрагмент кода нужно использовать?

правильный ответ:
++ 		: for file in file*.txt; do echo "$file"; done

ошибки:
-- for file in file*.text; do echo "$file"; done
-- for file in *.txt; do echo "$file"; done
-- for file in file.*.txt; do echo "$file"; done
-- for file in file?*.txt; do echo "$file; done"

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы анализируете конфигурацию Nginx, который размещён на публичном веб-сервере. Ниже приведен фрагмент файла /etc/ngnix/sites-enabled/default:
server {
	listen 80 default server;
	server_name example.com;
	root /var/www/html;
	index index.php index.html index.htm;
	location / {
	autoindex on;
	try_files $uri $uri/ =404;
	}
	location ~ \.php$ {
	include snippets/fastcgi-php.conf;
	fastcgi_pass unix:/run/php/php8.1-fpm.sock;
	}
}
Какая наиболее существенная проблема безопасности содержится в этой конфигурации?

правильный ответ:
++ 		: autoindex on раскрывает структуру директорий сервера при отсутствии index-файлов

ошибки:
-- Сервер обслуживает только example.com, поэтому остальные запросы будут отклонены
-- Используется Unix-сокет для PHP вместо TCP-порта, что небезопасно в продакшене
-- Неверно настроена директива try_files, что может вызвать ошибки 500
-- Отсутствует ограничение доступа к PHP-файлам, что позволяет выполнять произвольный код

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой из способов наиболее корректно применить новые правила iptables без обрыва активных подключений?

правильный ответ:
++ 		: iptables-restore < rules.v4

ошибки:
-- iptables-save
-- systemctl reload iptables
-- service iptables start

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Изучая вывод netstat , вы замечаете множество соединений в состоянии TIME_WAIT . Как это может сказаться на производительности?

правильный ответ:
++ 		: Приводит к исчерпанию локальных портов и увеличению задержек в установлении новых соединений

ошибки:
-- Не влияет на производительность
-- Вызывает утечку памяти
-- Сильно снижает скорость чтения с диска

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы анализируете задание в crontab:
30 2 * * 1-5 /usr/local/bin/backup.sh
Что выполняет это задание?

правильный ответ:
++ 		: Скрипт выполняется в 2:30 ночи только по будням 

ошибки:
-- Скрипт выполняется каждый понедельник и пятницу
-- Скрипт выполняется каждый день в 2:30 ночи
-- Скрипт выполняется каждую неделю в воскресенье

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы создаете systemd-таймер со следующей секцией:
[Timer]
OnBootSec=10min
Persistent=false
Что произойдет при включении системы?

правильный ответ:
++ 		: Задача запустится один раз через 10 минут после загрузки

ошибки:
-- Ничего не произойдет, пока не будет указан OnCalendar
-- Таймер будет активироваться каждые 10 минут после старта
-- Запуск произойдет при выключении системы
-- Задача запустится только при первой загрузке системы

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какое действие systemd выполнит после успешной активации unit'а с типом service и параметром After=network.target ?

правильный ответ:
++ 		: Запустит сервис только после завершения активации network.target

ошибки:
-- Запустит сервис параллельно с network.target .
-- Перезапустит сервис при каждой смене статуса сети.
-- Запустит сервис немедленно, игнорируя зависимость.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой командой можно перечитать изменения в /etc/fstab без перезагрузки системы?

правильный ответ:
++ 		: mount -a

ошибки:
-- mtab --reload
-- fstab -r
-- reboot

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Конфигурация sshd содержит:
AllowUsers admin
DenyUsers admin
Пользователь admin получает отказ при попытке входа. Почему?

правильный ответ:
++		: Конфликт директив разрешается в пользу DenyUsers, и вход блокируется

ошибки:
-- Такая конфигурация вызывает фатальную ошибку при старте sshd
-- AllowUsers всегда имеет приоритет над DenyUsers
-- Директивы AllowUsers и DenyUsers игнорируются при использовании PAM
-- Пользователь admin будет иметь доступ, если указан в Match

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
При монтировании NFS-ресурса в fstab часть данных исчезает в точке монтирования, но видна на сервере. В чем причина?

правильный ответ:
++ 		: Несовпадение uid/gid между клиентом и сервером

ошибки:
-- Использован параметр soft вместо hard
-- Некорректная маска прав доступа на экспорте
-- Отсутствует опция sync в монтировании

*Протокол NFS (Network File System) по умолчанию аутентифицирует пользователей на основе их числовых идентификаторов — UID (User ID) и GID (Group ID), а не по именам пользователей.

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
В journalctl зафиксировано следующее:
systemd[1]: myapp.service: Main process exited, code=exited, status=203/EXEC
Какой объяснение наиболее точно отражает сутьэтой ошибки?
Варианты ответа:
-- systemd не смог разрешить зависимости в секции [Install]
-- Служба завершилась из-за сигнала SIGKILL
-- Пользователь, указанный в Unit, не имеет доступа к systemctl
-- Файл, указанный в ExecStart=, не существует или недоступен для выполнения
-- Параметр TimeoutSec= слишком мал для запуска

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы хотитев реальном времени отслеживать только сообщения уровня warning и выше от службы nginx.service. Какую команду использовать?
Варианты ответа:
-- tail -f /var/log/nginx/error.log
-- journalctl -u nginx.service -p warning -f
-- journalctl -f -g warning
-- systemctl status nginx.service | grep warning
-- dmesg -u nginx.service --level=warning

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Почему следующая конструкция в Bash может некорректно обработать файлы с пробелами в имени?
for file in $(ls *.txt); do echo "$file"; done
Варианты ответа:
-- Переменная $file должна быть в одинарных кавычках
-- Команда ls разделяет вывод по пробелам, а не по именам файлов
-- Нельзя использовать echo с переменными в цикле
-- *.txt раскрывается некорректно без кавычек
-- Цикл for не поддерживает массивы

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой блок конструкций в скрипте позволяет перехватывать неуспешное выполнение и перейти к обработке ошибок?
Варианты ответа:
-- if ... then ... fi
-- for ... do ... done
-- while ... do ... done
-- trap ... ERR

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
В конфигурации Nginx отсутствует директива deny all; в location-блоке для ./git. Почему это представляет угрозу?
Варианты ответа:
-- Приводит к тому, что сервер запрещает доступ к index.html
-- Позволяет выполнять Git-команды через HTTP напрямую 
-- Вызывает повторную инициализацию репозитория на каждый запрос
-- Блокирует работу cron-скриптов, размещенных в корне
-- Позволяет загружать содержимое .git-каталога, включая историю коммитов и потенциальные секреты

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы анализируете вывод iostat -x и замечаете высокие значения %util (близко к 100%) и увеличенные await. Что можно заключить о текущем состоянии системы?
Варианты ответа:
-- Процессорное время полностью уходит на системные вызовы
-- Увеличение await свидетельствует о высоком использовании сети
-- Сервисов слишком много, и не хватает дескрипторов
-- Подсистема хранения перегружена, происходят задержки при операциях ввода-вывода
-- ОЗУ заполнена, и происходит активная подкачка

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какой инструмент применяется для выявления "узких мест" в работе дисковых подсистем?
Варианты ответа:
-- zip
-- curl
-- iostat
-- strace

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Вы создаете systemd-таймер с параметрами:
[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
Какой эффект оказывает RandomizedDelaySec в этом случае?
Варианты ответа:
-- Скрипт будет запускаться один раз с точной задержкой в 1 час
-- Функция игнорируется без установки AccuracySec
-- Таймер будет срабатывать каждый час после наступления daily
-- systemd проигнорирует RandomizedDelaySec для OnCalendar
-- Запуск будет случайно сдвинут на время до 1 часа после запланированного события

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
После обновления ядра и пакетов на сервере служба network перестала запускаться автоматически. При попытке вручную запустить службу network с помощью systemctl start возникает ошибка:
Job for network.service failed because the control process exited with error code.
See "systemctl status network.service" and "journalctl -xe" for details.
Команда systemctl status network.service показывает, что юнит существует, но не стартует. Какова наиболее вероятная причина этой проблемы?
Варианты ответа:
-- В конфигурации systemd отсутствует корректная зависимость от загрузки модулей ядра
-- Служба network конфликтует с новым системным менеджером NetworkManager, вызывая ошибку запуска
-- Драйвер сетевой карты несовместим с новым ядром и не загружается автоматически
-- Права доступа к файлам службы network.service были изменены, что препятствует ее запуску
-- Файл конфигурации network.service поврежден или содержит ошибку синтаксиса

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
Какое действие корректно инициировать для перехода системы в rescue mode с помощью systemd ?
Варианты ответа:
-- systemctl isolate multi-user.target
-- init 3
-- shutdown -r now
-- systemctl rescue

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
На сервере включен SELinux, и пользовательское приложение debug-agent работает в собственном домене debug_t. Приложению назначена capability CAP_SYS_PTRACE для анализа других процессов. Несмотря на SELinux-ограничения, debug-agent способен отслеживать процессы других доменов. Почему это возможно?
Варианты ответа:
-- SELinux не может изолировать процессы, если capability задана через setcap
-- Домен debug_t по умолчанию имеет атрибут permissive, если используется CAP_SYS_PTRACE
-- Права capabitilies автоматически отключают SELinux-контроль на уровне домена
-- Процессы одного пользователя всегда могут ptrace-друг друга, независимо от SELinux
-- CAP_SYS_PTRACE обходит дискреционные ограничения SELinux при отсутствии соответствующей политики ограничения доступа к другим доменам

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
В чем потенциальная проблема при использовании в пункте /etc/fstab для монтирования раздела идентификатора устройства (например, /dev/sda1) вместо UUID ?
Варианты ответа:
-- После добавления нового диска может измениться последовательность именований устройств
-- Идентификатор /dev/sda1 всегда уникален для всех систем 
-- Использование UUID может повлечь за собой потерю производительности
-- UUID не поддерживается большинством современных дистрибутивов

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
При настройке overlayfs была применена слудующая команда:
mount -t overlay overlay -o lowerdir=/opt/base,upperdir=/opt/overlay,workdir=/opt/work /mnt/merged
При монтировании часть содержимого /opt/base стала недоступна, а некоторые изменения не сохранились после перезагрузки. Что вероятнее всего, пошло не так? 
Варианты ответа:
-- mount overlay требует rootfs, иначе слои игнорируются
-- lowerdir был перекрыт overlay'ем, и базовая структура стала недоступна
-- В overlayfs всегда требуется fstab-запись, иначе теряются изменения
-- upperdir и workdir указывали на нестабильный том, и данные не были сброшены в постоянное хранилище
-- Отсутствие опции copy_up привело к потере данных при записи

 - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
